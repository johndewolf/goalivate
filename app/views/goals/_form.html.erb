<% @goals.each do |goal| %>
<%= simple_form_for goal do |f| %>
  <%= f.association :exercise %>
  <%= f.input :starting_strength %>
  <%= f.input :goal_weight %>
  <%= f.input :goal_date, as: :date  %>
  <%= f.button :submit %>
<% end %>
<% end %>
